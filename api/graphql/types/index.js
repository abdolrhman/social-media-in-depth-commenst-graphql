const { NoteType } = require('./NoteType');
const { UserType } = require('./UserType');
const { CommentType } = require('./CommentType');

module.exports = {
  NoteType,
  UserType,
  CommentType,
};
